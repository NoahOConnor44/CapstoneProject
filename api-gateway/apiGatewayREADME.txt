The API service handles taking front end requests and forwarding them to the proper back end service.
This setup allows for changes to requests be made at the API gateway instead of at the service while also handling
data being sent back for the front end. 